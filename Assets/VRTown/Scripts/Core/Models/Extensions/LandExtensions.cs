using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace VRTown.Model.Extensions
{
    public static class LandExtensions
    {
        public static bool Overlap(this Rect border, Rect otherBorder)
        {
            // float overlap = 0f;
            // foreach (var r1 in rects1)
            // {
            //     foreach (var r2 in rects2)
            //     {
            //         overlap += Overlap(r1, r2);
            //     }
            // }

            return true;
        }
    }
}